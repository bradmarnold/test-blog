---
import BaseLayout from './BaseLayout.astro';
const { frontmatter } = Astro.props;
const pageTitle = `${frontmatter.title} • Your Blog`;
---
<BaseLayout title={pageTitle} description={frontmatter.description}>
  <main class="mx-auto max-w-3xl px-4 sm:px-6">
    <article class="prose prose-zinc mt-12">
      <header class="not-prose mb-8">
        <h1 class="text-3xl font-semibold tracking-tight text-gray-900">{frontmatter.title}</h1>
        <div class="mt-3 flex items-center gap-3 text-sm text-gray-500">
          {frontmatter.publishedDate && (
            <time datetime={frontmatter.publishedDate}>
              {new Date(frontmatter.publishedDate).toLocaleDateString(undefined, { year: 'numeric', month: 'short', day: 'numeric' })}
            </time>
          )}
          {frontmatter.tags?.length > 0 && (
            <ul class="flex flex-wrap gap-2">
              {frontmatter.tags.map((t: string) => <li class="rounded-full border border-accent-200 px-2 py-0.5 text-accent-700">{t}</li>)}
            </ul>
          )}
        </div>
      </header>
      <slot />
    </article>
  </main>
</BaseLayout>